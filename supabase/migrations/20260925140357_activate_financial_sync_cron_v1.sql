-- Ativação operacional executada diretamente no projeto Supabase de produção em 25/09/2026.
-- O job pdde-financial-sync-v1 foi configurado com token gerado no banco e acionado uma vez
-- para sincronização imediata. Este arquivo existe apenas para reconciliar o histórico remoto
-- de migrations sem reproduzir chamadas de produção em ambientes locais ou de preview.
select 1;
